name: Deploy Laravel App

on:
  push:
    branches:
      - main
      - staging
      - develop
  pull_request:
    branches:
      - main
      - staging
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, bcmath, pdo, mysql
          coverage: none

      - name: Copy environment file
        run: |
          if [[ "${{ github.base_ref }}" == "main" ]]; then
            cp .env.production .env
          elif [[ "${{ github.base_ref }}" == "staging" ]]; then
            cp .env.staging .env
          elif [[ "${{ github.base_ref }}" == "develop" ]]; then
            cp .env.develop .env
          fi

      - name: Install dependencies
        run: composer install --no-progress --no-interaction --prefer-dist --optimize-autoloader

      - name: Generate application key
        run: php artisan key:generate

      - name: Run migrations (optional)
        run: php artisan migrate --force

      #- name: Deploy to server (example using rsync)
      #  run: |
      #    if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
      #      ssh ${{ secrets.PROD_SSH_USER }}@${{ secrets.PROD_HOST }} "cd /var/www/html && git pull origin main && php artisan config:cache"
      #    elif [[ "${{ github.ref }}" == "refs/heads/staging" ]]; then
      #      ssh ${{ secrets.STAGING_SSH_USER }}@${{ secrets.STAGING_HOST }} "cd /var/www/staging && git pull origin staging && php artisan config:cache"
      #    fi
